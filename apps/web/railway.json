{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build --workspace packages/core && npm run build --workspace packages/rules && npm run build --workspace apps/web"
  },
  "deploy": {
    "startCommand": "npm run start --workspace apps/web",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
