{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace packages/core && npm run build --workspace packages/rules && npm run build --workspace apps/worker"
  },
  "deploy": {
    "startCommand": "npm run db:migrate --workspace packages/core && npm run start --workspace apps/worker",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
